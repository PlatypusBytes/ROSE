# This is a sample build configuration for Python.
# Check our guides at https://confluence.atlassian.com/x/x4UWN for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: continuumio/miniconda3:latest

clone:
  lfs: true

pipelines:
  default:
    - step:
        name: Install requirements.
        caches:
          - conda
          - pip
        script: # Modify the commands below to build your repository.
          - conda env update -n rose --file env_rose.yml
    - parallel:
        - step: # Run tests
            name: Run tests and codecoverage.
            caches:
                - conda
                - pip
            script:
                - conda env update -n rose --file env_rose.yml
                - pip install pybadges
                - conda activate rose
                - pytest -v --cov=src --cov-report html:test-reports/coverage src/tests/ -m "not workinprogress"
                - python -m pybadges --left-text=build --right-text=failure --right-color='#c00' > test-reports/status.svg
                - apt-get --yes install zip
                - zip -r test-reports/coverage.zip test-reports/coverage
                - pipe: atlassian/bitbucket-upload-file:0.3.1
                  variables:
                      BITBUCKET_USERNAME: $BITBUCKET_USERNAME
                      BITBUCKET_APP_PASSWORD: $BITBUCKET_APP_PASSWORD
                      FILENAME: "test-reports/coverage.zip"
                - pipe: atlassian/bitbucket-upload-file:0.3.1
                  variables:
                      BITBUCKET_USERNAME: $BITBUCKET_USERNAME
                      BITBUCKET_APP_PASSWORD: $BITBUCKET_APP_PASSWORD
                      FILENAME: "test-reports/status.svg"
        
definitions:
   caches:
     conda: ~/.conda/envs/rose
     codatarballs: /opt/conda/pkgs